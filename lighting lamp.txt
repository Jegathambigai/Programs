// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>
int main() {
    int n,i,j,b=0;
    char* a[100][100];
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        for(j=1;j<=n;j++){
            if(i==1||j==1||i<j){
                a[i][j]="on ";
            }
            else if(i>1&&i>j){
                a[i][j]="off ";
            }
            else if(i==j){
                a[i][j]="off ";
            }
        }
    }
    for(i=1;i<=n;i++){
        for(j=1;j<=n;j++){
            printf("%s",a[i][j]);
        }printf("\n");
    }
  
        for(j=1;j<=n;j++){
            if(a[n][j]=="on "){
                b=b+1;
            }
        }
        printf("count:%d",b);
    
    
    return 0;
}